services:
  server:
    build:
      context: ./server
    container_name: step4-proxy-server
    expose:
      - "4000"

  nginx:
    build:
      context: ./nginx
    container_name: step4-proxy-nginx
    ports:
      - "8080:80"
    depends_on:
      - server
